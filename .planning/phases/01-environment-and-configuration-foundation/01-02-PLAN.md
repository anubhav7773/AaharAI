---
phase: 01-environment-and-configuration-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - mobile/lib/core/config/app_env.dart
  - mobile/lib/core/network/api_client.dart
  - mobile/lib/main.dart
  - mobile/.env.example
  - mobile/.gitignore
  - mobile/test/core/config/app_env_test.dart
  - mobile/test/widget_test.dart
  - mobile/README.md
autonomous: true
requirements:
  - CONF-02
  - CONF-03
user_setup: []
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A mobile build selects its backend URL and public Supabase settings through documented dart-define keys without source edits."
    - "The mobile configuration boundary rejects empty or malformed required values before the API client is constructed."
    - "The local API URL default is documented as an emulator-only development default and deployment values are never embedded as mutable source configuration."
    - "Mobile real env files are ignored while the public example template remains trackable."
  artifacts:
    - path: "mobile/lib/core/config/app_env.dart"
      provides: "Validated compile-time AppEnv value object"
      contains: "String.fromEnvironment"
    - path: "mobile/lib/core/network/api_client.dart"
      provides: "Dio client configured from AppEnv rather than a hardcoded deployment URL"
      contains: "AppEnv"
    - path: "mobile/.env.example"
      provides: "Documented public build-input template"
      contains: "API_BASE_URL"
    - path: "mobile/test/core/config/app_env_test.dart"
      provides: "AppEnv validation tests"
  key_links:
    - from: "mobile/lib/main.dart"
      to: "mobile/lib/core/config/app_env.dart"
      via: "startup validation before ProviderScope/runApp"
      pattern: "AppEnv"
    - from: "mobile/lib/core/network/api_client.dart"
      to: "mobile/lib/core/config/app_env.dart"
      via: "Dio BaseOptions.baseUrl receives validated AppEnv.apiBaseUrl"
      pattern: "baseUrl"
    - from: "mobile/README.md"
      to: "mobile/lib/core/config/app_env.dart"
      via: "same exact dart-define key names and defaults"
      pattern: "API_BASE_URL"
---

<objective>
Replace the existing hardcoded mobile deployment endpoint with validated compile-time configuration and document the safe build contract.

Purpose: The existing Dio client points directly at the Render URL, while CONF-03 requires environment selection through --dart-define and only public Supabase values on the client.
Output: AppEnv boundary, API-client/bootstrap wiring, safe template and ignore rules, deterministic tests, and reproducible build instructions.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-environment-and-configuration-foundation/01-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@aahar_ai_docs/Doc_01_Product_Architecture_Rules.md
@aahar_ai_docs/Doc_08_Flutter_State_Navigation.md
@mobile/lib/core/network/api_client.dart
@mobile/lib/main.dart
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Trace Dart defines from app startup into the existing Dio boundary</name>
  <precondition>Plan 01 has produced the backend configuration contract and its focused tests pass.</precondition>
  <files>mobile/lib/core/config/app_env.dart, mobile/lib/core/network/api_client.dart, mobile/lib/main.dart</files>
  <action>Introduce a const AppEnv value object under mobile/lib/core/config/app_env.dart using the exact public keys API_BASE_URL, SUPABASE_URL, and SUPABASE_ANON_KEY. Read them with Dart compile-time environment declarations, allow only the documented emulator-local API default of http://10.0.2.2:8000, require the two public Supabase values unless an explicitly documented test constructor is used, validate URL scheme/host and non-empty values, and produce errors containing variable names rather than values. Wire startup to validate the environment before runApp and change ApiClient construction so Dio receives the validated AppEnv API URL; preserve current timeout, headers, retry, and endpoint behavior. Do not add a runtime dotenv loader and do not place Gemini or Supabase service-role credentials in Dart code or defines.</action>
  <verify>
    <automated>flutter --project-dir mobile test test/core/config/app_env_test.dart</automated>
    <fails_when>Fails when the focused Flutter configuration tests return a non-zero exit code or any required validation case is not executed successfully.</fails_when>
  </verify>
  <done>The existing mobile API path is driven by the validated AppEnv contract, startup cannot construct the client with invalid required configuration, and the Android emulator default remains explicit and non-production.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove mobile configuration validation and preserve widget-test determinism</name>
  <files>mobile/test/core/config/app_env_test.dart, mobile/test/widget_test.dart</files>
  <behavior>
    - Test 1: Explicit valid API URL, Supabase URL, and anon key create an AppEnv with matching values.
    - Test 2: Empty required public values and malformed/non-http API URLs are rejected with variable-oriented errors.
    - Test 3: The documented API default is accepted only as the local emulator default and does not silently substitute missing public Supabase values.
    - Test 4: Existing widget tests construct the app through the test-safe AppEnv path and do not depend on developer dart-defines.
  </behavior>
  <action>Implement focused flutter_test coverage using the injectable/test constructor or factory selected in AppEnv, while keeping production construction compile-time and const-capable. Update the existing widget smoke test only as needed to supply deterministic test values; do not weaken production validation to make tests pass. Assert that error messages identify missing or malformed define names without echoing supplied values.</action>
  <verify>
    <automated>flutter --project-dir mobile test test/core/config/app_env_test.dart; flutter --project-dir mobile test</automated>
    <fails_when>Fails if either the focused or full Flutter test command returns a non-zero exit code; no command failure may be swallowed.</fails_when>
  </verify>
  <done>Focused and full Flutter tests pass without developer environment state, and all required mobile configuration validation cases are executable.</done>
</task>

<task type="auto">
  <name>Task 3: Document public build inputs and enforce mobile secret-file hygiene</name>
  <files>mobile/.env.example, mobile/.gitignore, mobile/README.md</files>
  <action>Create a safe mobile template that clearly labels API_BASE_URL, SUPABASE_URL, and SUPABASE_ANON_KEY as public/build inputs, explains that the example file is not automatically loaded by Flutter, documents the emulator default and the exact flutter run/build --dart-define commands, and states that backend-only Gemini and service-role settings never belong in mobile configuration. Extend the existing Flutter ignore rules to exclude local .env variants while negating only .env.example. Keep documentation names identical to AppEnv and ensure no live endpoint credential or server secret is introduced.</action>
  <verify>
    <automated>python -c 'import subprocess,sys; ignored=subprocess.run(["git","check-ignore","-q","mobile/.env"]).returncode==0; example_ignored=subprocess.run(["git","check-ignore","-q","mobile/.env.example"]).returncode==0; sys.exit(0 if ignored and not example_ignored else 1)'; flutter --project-dir mobile analyze; git diff --check</automated>
    <fails_when>Fails if the mobile env status assertion, Flutter analyzer, or git diff --check returns non-zero; mobile/.env must be ignored and mobile/.env.example must remain unignored.</fails_when>
  </verify>
  <done>CONF-02 and CONF-03 are documented and mechanically verifiable: mobile local env files are ignored, the example is trackable, analyzer passes, and documented dart-define commands match the code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Build command → Flutter binary | Compile-time values enter the mobile application configuration. |
| Mobile app → backend/Supabase | Public endpoint and anonymous Supabase values cross into client runtime; server secrets must not. |
| Repository/index ← mobile env files | Local developer configuration may contain environment-specific values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-03 | Information disclosure | mobile/lib/core/config/app_env.dart and templates | high | mitigate | Restrict mobile inputs to backend URL and public/anonymous Supabase values; test and document exclusion of backend-only credentials. |
| T-01-04 | Tampering | AppEnv validation and Dio initialization | medium | mitigate | Validate required values and URL shape before client construction; cover malformed and missing define cases. |
| T-01-05 | Information disclosure | mobile/.env | medium | mitigate | Ignore local env files and keep only a safe example template trackable. |
</threat_model>

<verification>
From the repository root, run `flutter --project-dir mobile test test/core/config/app_env_test.dart`, `flutter --project-dir mobile test`, `flutter --project-dir mobile analyze`, both mobile git check-ignore assertions, and `git diff --check`.
</verification>

<success_criteria>
CONF-02 and CONF-03 are evidenced by deterministic AppEnv tests, no hardcoded deployment URL in the API-client configuration path, matching build documentation/templates, and passing Flutter analysis/tests.
</success_criteria>

<output>
Create `.planning/phases/01-environment-and-configuration-foundation/01-02-SUMMARY.md` when done.
</output>
