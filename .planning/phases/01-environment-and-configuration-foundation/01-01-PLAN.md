---
phase: 01-environment-and-configuration-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/app/__init__.py
  - backend/app/core/__init__.py
  - backend/app/core/config.py
  - backend/.env.example
  - backend/.gitignore
  - backend/tests/test_config.py
autonomous: false
requirements:
  - CONF-01
  - CONF-02
user_setup: []
estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A backend process can construct typed settings from backend/.env when all required values are present."
    - "Backend startup/configuration construction fails with a clear variable-oriented validation error when a required Gemini or Supabase value is absent."
    - "Real backend environment files are ignored while the backend example file remains trackable and contains no live credential."
  artifacts:
    - path: "backend/app/core/config.py"
      provides: "Single Pydantic Settings boundary for Gemini and Supabase configuration"
      contains: "SettingsConfigDict"
    - path: "backend/requirements.txt"
      provides: "Pinned settings foundation dependencies"
      contains: "pydantic-settings"
    - path: "backend/.env.example"
      provides: "Safe backend configuration template"
      contains: "GEMINI_API_KEY"
    - path: "backend/tests/test_config.py"
      provides: "Focused required-value and dotenv loading tests"
  key_links:
    - from: "backend/app/core/config.py"
      to: "backend/.env"
      via: "Pydantic Settings dotenv source configured for the backend working directory"
      pattern: "env_file"
    - from: "backend/tests/test_config.py"
      to: "backend/app/core/config.py"
      via: "isolated Settings construction with valid and missing environment values"
      pattern: "Settings"
---

<objective>
Create the backend configuration boundary and secret-file hygiene needed by every later backend phase.

Purpose: The repository has no backend directory, and the supplied architecture requires Gemini and Supabase service-role settings to remain server-side and fail loudly when absent.
Output: Importable backend settings package, dependency manifest, safe env template/ignore rules, and focused tests.
</objective>

<execution_context>
@~/.copilot/gsd-core/workflows/execute-plan.md
@~/.copilot/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-environment-and-configuration-foundation/01-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@aahar_ai_docs/Doc_01_Product_Architecture_Rules.md
@aahar_ai_docs/Doc_03_Backend_FastAPI_Endpoints.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify suspicious Pydantic packages before installation</name>
  <what-built>Package legitimacy gate for the two research-identified SUS packages required by the backend settings boundary.</what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/pydantic/ and confirm the package is the official Pydantic project linked to github.com/pydantic/pydantic.
    2. Open https://pypi.org/project/pydantic-settings/ and confirm the package is the official Pydantic settings project linked to github.com/pydantic/pydantic-settings.
    3. Approve only if both registry identities and source links are legitimate; otherwise stop and report the package concern.
  </how-to-verify>
  <resume-signal>Type "approved" after both packages are verified, or report the package that failed verification.</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: Trace required backend settings from dotenv input to typed runtime object</name>
  <files>backend/requirements.txt, backend/app/__init__.py, backend/app/core/__init__.py, backend/app/core/config.py, backend/.env.example, backend/.gitignore</files>
  <action>After Task 1 approval, create the backend package and install only the approved pinned Pydantic/Pydantic Settings dependencies from backend/requirements.txt. Implement one Settings(BaseSettings) boundary in backend/app/core/config.py using the exact server-side names GEMINI_API_KEY, SUPABASE_URL, and SUPABASE_SERVICE_ROLE_KEY from the architecture docs, with dotenv loading from backend/.env when commands run from backend, typed required values, extra="ignore", and a cached settings accessor for later FastAPI consumers. Keep secrets out of imports, logs, exceptions, templates, and Dart-facing configuration. Add backend/.env.example with explanatory safe placeholders and backend/.gitignore rules that ignore local env variants while explicitly keeping .env.example trackable. The tracer must prove the real path by constructing settings from a temporary dotenv file and by failing through Pydantic validation when a required value is absent; do not use scattered os.getenv calls.</action>
  <verify>
    <automated>python -m pip install -r backend/requirements.txt; python -c "from backend.app.core.config import Settings; print(Settings.model_fields.keys())"</automated>
  </verify>
  <done>Backend settings imports successfully, exposes all three required configuration fields, reads a backend dotenv source, and has no secret values in tracked templates or source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock backend configuration failure and repository hygiene with tests</name>
  <files>backend/tests/test_config.py</files>
  <behavior>
    - Test 1: A temporary dotenv file containing all three required values produces a Settings object with matching values.
    - Test 2: Omitting each required setting produces a validation error naming the missing variable without exposing any credential value.
    - Test 3: The settings boundary honors the documented backend dotenv source and ignores unrelated environment keys.
  </behavior>
  <action>Write isolated pytest tests that construct the settings class with controlled environment/file inputs rather than importing a global singleton before monkeypatching. Assert the actual Pydantic validation exception and variable names, not merely a truthy result. Include a repository-facing check that backend/.env is ignored and backend/.env.example is not ignored; never place real values in fixtures. The tests must remain deterministic on a machine with no developer credentials.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_config.py -q; git check-ignore -v backend/.env; git check-ignore -v backend/.env.example; git diff --check</automated>
  </verify>
  <done>Focused backend tests pass for valid dotenv loading, each missing required value, and source behavior; ignore checks prove real env files are excluded while the example remains available.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Backend process ← dotenv/environment | Local or deployment environment values enter typed application configuration. |
| Repository/index ← env files | Developer-created files may contain service credentials and must not enter version control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Information disclosure | backend/.env and Settings errors | high | mitigate | Ignore real env variants, track placeholders only, and assert validation reports names without values. |
| T-01-02 | Tampering | backend/app/core/config.py | medium | mitigate | Centralize typed required-field validation and test missing values with controlled inputs. |
| T-01-SC | Tampering | pip installs | high | mitigate | Verify both SUS package identities on PyPI in the blocking human checkpoint before installation. |
</threat_model>

<verification>
From the repository root, run the focused pytest command, ignore checks, and `git diff --check`. The final phase gate also runs `python -m pytest backend/tests/test_config.py -q`, `flutter --project-dir mobile test test/core/config/app_env_test.dart`, `flutter --project-dir mobile test`, and `flutter --project-dir mobile analyze`.
</verification>

<success_criteria>
CONF-01 and the backend portion of CONF-02 are evidenced by passing focused tests, clear missing-variable validation, and clean ignore/template checks with no committed deployment secret.
</success_criteria>

<output>
Create `.planning/phases/01-environment-and-configuration-foundation/01-01-SUMMARY.md` when done.
</output>
