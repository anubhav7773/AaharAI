# Public build inputs only. Flutter does not load this file automatically.
# Pass these values with --dart-define when running or building the app.
API_BASE_URL=http://10.0.2.2:8000
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=replace-with-public-anon-key

# GEMINI_API_KEY and SUPABASE_SERVICE_ROLE_KEY are backend-only secrets.
# Never put them in this file, Dart code, or a mobile build command.
